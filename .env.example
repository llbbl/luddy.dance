# Environment Configuration for luddy.dance

# Node Environment
# - development: Enables logging, development features
# - production: Disables logging unless explicitly enabled
NODE_ENV=development

# Logging Configuration
# Set to 'true' to force enable logging in production
# By default, logging is disabled in production for performance
# NEXT_PUBLIC_ENABLE_LOGGING=true

# Next.js Configuration
# NEXT_PUBLIC_ prefix makes variables available in the browser
# Use for features that need client-side environment detection