# Development

This site has no application build step or package manager. Edit `index.html`, `styles.css`, and `app.js` directly.

For a production-equivalent preview, run:

```bash
docker compose up --build
```

Open [localhost:8080](http://localhost:8080). Stop it with `docker compose down`.
